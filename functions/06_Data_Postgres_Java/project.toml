[_]
schema-version = "0.2"

[com.salesforce]
schema-version = "0.1"
id = "postgresjava"
description = "Connects to a PostgreSQL instance, stores the invocation ID, and returns a list of previous invocations."
type = "function"
salesforce-api-version = "55.0"
